# jjozzietech-playground

Personal sandbox while I learn git and GitHub workflows.

## About

This is throwaway. I'm using it to practice the clone → edit → commit → push loop
before working on real repositories.

## Notes

- Created June 2026
- WSL2 Ubuntu 26.04 + VS Code on Windows
- Practising git workflow as part of preparing public Akash provider work

## What I've learned so far

- `git status` is your most-used command — run it before and after anything
- `git add` stages changes; `git commit` records them locally; `git push` sends them to GitHub
- Every commit needs a message; write it so future-you knows what it did