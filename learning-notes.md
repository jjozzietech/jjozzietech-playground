# Learning notes

A scratch file for git/markdown experiments.

## Today (June 29, 2026)

- Set up WSL2 Ubuntu 26.04 on Windows workstation
- Generated SSH key, registered with GitHub, 2FA enabled
- Installed VS Code with WSL extension
- First clone, edit, commit, push loop on this playground repo
- Realised `git status` is the safety net — run it twice per operation